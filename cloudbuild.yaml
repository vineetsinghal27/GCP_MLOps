steps:
# Step 1: Install Dependenices + Run Training Code
- name: 'python:3.10'
  entrypoint: 'bash'
  args:
    - '-c'
    - |
      pip install -r requirements.txt
      python train.py

# Step 2: Upload model to GCS
- name: 'gcr.io/cloud-builders/gsutil'
  args: 
    - 'cp'
    - 'outputs/model.pkl'
    - 'gs://credit-card-fraud-gcp/'

# Step 4: Upload metrics
- name: 'gcr.io/cloud-builders/gsutil'
  args:
    - 'cp'
    - 'outputs/metrics.json'
    - 'gs://credit-card-fraud-gcp/'

options:
   logging: CLOUD_LOGGING_ONLY

